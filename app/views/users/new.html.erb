<% if flash[:error] %>
  <% flash[:error].each do |error| %>
    <div class="alert alert-danger">
      <%= error %>
    </div>
  <% end %>
<% end %>

<h1>Sign Up</h1>

<%= render 'new_form' %>

<br>
<%= link_to "Or sign in with Google", "/auth/google_oauth2" %>
