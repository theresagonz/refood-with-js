<h1 class="slab-font"><%= @offer.closed ? "Closed" : "Open" %> Offer</h1>
<h3><%= link_to @offer.headline, offer_path(@offer) %></h3>
<div class="small grey-text">Posted on <%= format_date(@offer.created_at.localtime) %></div>
