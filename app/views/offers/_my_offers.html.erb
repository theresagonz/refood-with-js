<% if @offers.present? %>
  <ul class="list-group">
  <% @offers.each do |offer| %>
    <li class="list-group-item">
      <h5><%= link_to offer.headline, offer_path(offer) %></h5>
      <% if offer.requests.any? { |r| r.completed_giver == true && r.completed_requestor == false } %>
        <span class="badge badge-warning">New activity</span><br>
      <% end %>
      <%= link_to "#{offer.requests.select { |r| !r.completed_requestor }.count} open requests", offer_requests_path(offer) %><br>
      <span class="small grey-text">posted <%= time_ago(offer) %> ago</span>
    </li>
  <% end %>
  </ul>
<% else %>
  <h5>No open offers</h5>
<% end %>
