<%= render 'layouts/flash_message' %>

<%= render 'offers/headline' %>

<br>
<% if @requests.present? %>
  <% @requests.each do |req| %>
    <%= render 'display_request', :req => req %>
  <% end %>
<% else %>
  <h3 class="slab-font">No open requests</h3>
<% end %>

<br>
<h5 class="slab-font"><%= link_to "Completed requests", offer_completed_path %></h5>

<%= render 'welcome/link_to_home' %>
