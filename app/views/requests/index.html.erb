<h1>Requests for <%= @offer.title %></h1>
<ul class="list-group">
  <% @requests.each do |request| %>
    <li class="list-group-item">
      <%= link_to request.message, offer_request_path(@offer, request) %><br>  
      Name: <%= request.receiver.user.name %><br>
      <% if request.receiver_email.present? %>
        Email: <%= request.receiver_email %><br>
      <% end %>
      <% if request.receiver_phone.present? %>
        Phone: <%= request.receiver_phone %><br>
      <% end %>
    </li>
  <% end %>
</ul>
